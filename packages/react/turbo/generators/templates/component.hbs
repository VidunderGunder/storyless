import * as React, { type ComponentPropsWithoutRef } from "react";
import { forwardRef } from "react";

export type Props = {
  // Custom props here
} & ComponentPropsWithoutRef<"div">; // Extend the props of the component you're wrapping here

const {{ pascalCase name }} = forwardRef<HTMLDivElement, Props>(function {{ pascalCase name }}(
  { children, ...props },
  ref
) {
  return (
    <div ref={ref} {...props}>
      <h1>{{ name }}</h1>
      {children}
    </div>
  );
});

export default {{ pascalCase name }};
