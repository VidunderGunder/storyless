import * as React from "react";
import { forwardRef } from "react";

export type {{ pascalCase name }}Props = {
  // Custom props here
} & React.ComponentPropsWithoutRef<"{{ element }}">;

export const {{ pascalCase name }} = forwardRef<HTML{{ pascalCase name }}Element, {{ pascalCase name }}Props>(function {{ pascalCase name }}(
  { children, ...props },
  ref
) {
  return (
    <{{ element }} ref={ref} {...props}>
      {children}
    </{{ element }}>
  );
});
