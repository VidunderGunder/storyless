{
  "private": true,
  "engineStrict": true,
  "engines": {
    "bun": ">=1.0.11"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "tsconfig": "*",
    "turbo": "latest"
  },
  "name": "storyless",
  "packageManager": "bun@1.0.11",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
